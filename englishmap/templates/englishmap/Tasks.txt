Home:
- погираться со вставкой template в django (цикл)

push modif
annotation/plans for nearest future

settings => static root
requirements (pip freeze)
gitignore
proc file
settings=> allowed hosts
install and import/set django-heroku
!git init, remote

heroku run manage.py migrate
heroku run bash => python manage.py createsuperuser
ViktorV superadmin12345

later: [set secret key] and debug value in heroku
? install postgres


Then:
- Deploy

- настроить классы в остальных странцах (фак, суппорт, демо-топик) (other pages and whatnot)
вместо inline css и почего дерьма
- настроить title на страницах
- # id в карточках
- top-margin (навбар + контент?) - отступ от верха

Then:
- почистить говно, сразу привести классы в порядок, убрать inline css и лишние комменты
- remake arrows (slightly), разобраться с шириной ячеек в гриде
- запушить изменения




Later:
- Footer always at the bottom
- Dynamic title
- Think about different perspectives: cordova app, server, routing, vue, how much python and js and vue do i want?
- think how to rework the js layout+data script into python 

- change JS method and data into python? Or not?
- use more suitable shades of red and other colours 

- model for topic_detail
- model for topic_update
- make only 1 actual modal where we feed info from the data
- make a special list [1-3] [4-6] to layout topics
- grid iterates, checks this list and automatically inserts arrows 
and info based on the spec. list

- your questions and feedback




Which text editor(excel, word, etc.) or database to use? Wysiwig window of CKeditor?
- for every rule (excel? by the levels):
ID (number) level# plus rule number like 101 (l1 number 1), 216 (l2 num 16)
Header (name)
Short description ()
Footer (wanna learn more) (-static-)
+
Full description, examples ()
Additional stuff (iframe vid, audio)
Links ()

- a dynamically created page (slug)
Header (name)
Full description, examples ()
Additional stuff (iframe vid, audio)
Links ()


- random padding - experiment with it (margin maybe? modal fixed size?)
- Base - crop the image to fit nicely (dynamic css variable?)?